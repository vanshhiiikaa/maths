#Meadian

import numpy as np
range_of_arr = np.array([10,20,30,40,50])
range = (np.max(range_of_arr) - np.min(range_of_arr))
print(range)

#OR

rangee = np.ptp(range_of_arr)

#Meadian is used if the numbers in an array has a huge difference